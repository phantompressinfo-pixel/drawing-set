# Office standards source documents

Empty right now. The `office-standards-qa` skill can only answer questions
from what's actually in this folder — add the real documents here.

What to add:

- Drawing standards (sheet naming/numbering, layer conventions, line
  weights, title block usage).
- Templates (or a doc describing which template applies when).
- Deliverable/submission checklists.
- File naming conventions.

Same rule as the building-code folder: if a standard changes, replace the
old document rather than leaving an outdated version alongside the current
one — the assistant has no way to know which one is current otherwise.
